# Asset Track

Envía datos de telemetría para un activo a ModularIoT.

## Endpoint

```
POST https://iot.streamhub.cl/v1/asset/track
```

## Autenticación

Requiere un token Bearer válido con scope `asset:track:write`.

```
Authorization: Bearer <access_token>
```

## Solicitud

### Headers

| Header | Requerido | Descripción |
|--------|-----------|-------------|
| `Authorization` | Sí | `Bearer <access_token>` |
| `Content-Type` | Sí | `application/json` |
| `Accept` | Recomendado | `application/json` para respuestas JSON |
| `X-Request-Id` | Sí | Identificador único para trazabilidad de la solicitud |
| `X-Request-Timestamp` | Sí | Timestamp de la solicitud (segundos Unix epoch) |

### Body (AssetTrackingData)

```json
{
  "asset_id": "A123456789",
  "timestamp": "2024-10-22T14:23:45Z",
  "type": "Truck",
  "owner": "Empresa ABC",
  "year": 2020,
  "gps": {
    "latitude": -33.4489,
    "longitude": -70.6693,
    "altitude": 520,
    "speed": 65.5,
    "heading": 180
  },
  "metrics": {
    "schema": "miot.metrics@1.0",
    "items": [
      { "k": "engine.rpm", "v": 2500, "u": "rpm", "src": "obd2", "q": "ok" },
      { "k": "vehicle.speed", "v": 72, "u": "km/h", "src": "obd2", "q": "ok" },
      { "k": "fuel.level", "v": 65, "u": "%", "src": "obd2", "q": "ok" }
    ],
    "seq": 12345
  }
}
```

### Referencia de Campos

| Campo | Tipo | Requerido | Descripción |
|-------|------|-----------|-------------|
| `asset_id` | string | **Sí** | Identificador único del activo |
| `timestamp` | string | **Sí** | Timestamp ISO 8601 con zona horaria |
| `type` | string | No | Tipo de activo (ej., "Truck", "Container") |
| `owner` | string | No | Nombre del propietario del activo |
| `year` | integer | No | Año de fabricación |
| `gps` | object | No | Datos de ubicación GPS |
| `telecom` | object | No | Información de conectividad |
| `sensors` | object | No | Lecturas de sensores |
| `driver_info` | object | No | Información del conductor |
| `co_driver_info` | object | No | Información del copiloto |
| `peripherals` | object | No | Periféricos conectados |
| `metrics` | object | No | Métricas canónicas (miot.metrics@1.0) |
| `events` | array | No | Eventos discretos |

### Objeto GPS

```json
{
  "latitude": -33.4489,
  "longitude": -70.6693,
  "altitude": 520,
  "speed": 65.5,
  "heading": 180,
  "accuracy": 5
}
```

| Campo | Tipo | Unidad | Descripción |
|-------|------|--------|-------------|
| `latitude` | number | grados | Latitud WGS84 (-90 a 90) |
| `longitude` | number | grados | Longitud WGS84 (-180 a 180) |
| `altitude` | number | metros | Altitud sobre el nivel del mar |
| `speed` | number | km/h | Velocidad sobre el suelo |
| `heading` | number | grados | Dirección (0-360) |
| `accuracy` | number | metros | Precisión del GPS |

### Objeto Metrics (miot.metrics@1.0)

Para enviar métricas canónicas de vehículos/IoT usando el sobre estandarizado:

```json
{
  "schema": "miot.metrics@1.0",
  "items": [
    { "k": "engine.rpm", "v": 2150, "u": "rpm", "src": "obd2", "q": "ok" },
    { "k": "engine.coolant_temp", "v": 91, "u": "C", "src": "obd2", "q": "ok" },
    { "k": "battery.voltage", "v": 13.9, "u": "V", "src": "obd2", "q": "ok" },
    { "k": "vehicle.speed", "v": 65, "u": "km/h", "src": "obd2", "q": "ok" }
  ],
  "seq": 12345,
  "device_ts": "2024-10-22T14:23:45Z"
}
```

#### Campos de MetricsEnvelope

| Campo | Tipo | Requerido | Descripción |
|-------|------|-----------|-------------|
| `schema` | string | **Sí** | Debe ser `"miot.metrics@1.0"` |
| `items` | array | **Sí** | Array de objetos MetricItem (mín 1, máx 200) |
| `seq` | integer | No | Número de secuencia del dispositivo para ordenamiento/deduplicación |
| `device_ts` | string | No | Timestamp del reloj del dispositivo (RFC3339) |
| `capabilities` | object | No | Metadatos opacos sobre métricas soportadas |

#### Campos de MetricItem

| Campo | Tipo | Requerido | Descripción |
|-------|------|-----------|-------------|
| `k` | string | **Sí** | Clave de métrica canónica (ej., `engine.rpm`) |
| `v` | any | **Sí** | Valor (número, booleano, string, objeto, o array) |
| `u` | string | No | Unidad canónica (ej., `rpm`, `km/h`, `C`, `V`, `%`) |
| `ts` | string | No | Timestamp override para este item (RFC3339) |
| `src` | string | No | Fuente: `obd2`, `j1939`, `can`, `oem`, `derived`, `unknown` |
| `q` | string | No | Calidad: `ok`, `estimated`, `stale`, `invalid`, `missing` |
| `meta` | object | No | Metadatos opacos para trazabilidad (PID, SPN, CAN ID) |

> **Patrón de claves:** Las claves de métricas deben coincidir con `^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*){1,5}$` (2-6 segmentos en minúsculas separados por puntos)

→ Ver **Referencia > Registro de Métricas** para todas las claves de métricas canónicas disponibles.

### Objeto Sensors

Para dispositivos GPS que envían datos de sensores directamente (sin conexión OBD-II o CAN BUS):

```json
{
  "odometer": 125432.5,
  "engine_status": 1
}
```

| Campo | Tipo | Descripción |
|-------|------|-------------|
| `odometer` | number | Distancia total recorrida en kilómetros |
| `engine_status` | integer | Estado del motor: `1` = encendido, `0` = apagado |

> Use este objeto cuando el dispositivo GPS proporciona lecturas de odómetro y estado del motor directamente, sin depender del bus de diagnóstico del vehículo.

---

## Respuesta

### Éxito (200 OK)

```json
{
  "status": "success",
  "message": "Message sent successfully"
}
```

### Error de Validación (400 Bad Request)

```json
{
  "status": "validation_error",
  "message": "Request validation failed",
  "errors": {
    "assetId": "Asset ID is required and cannot be blank",
    "timestamp": "Timestamp is required"
  }
}
```

### No Autorizado (401)

Se devuelve cuando el token es inválido o ha expirado.

### Error Interno (500)

```json
{
  "status": "error",
  "message": "Failed to publish message",
  "detail": "Connection timeout"
}
```

---

## Colección Postman

Descarga nuestra colección de Postman lista para usar con todos los ejemplos:

<a href="/api/downloads/postman" download="miot-metrics-postman-collection.json" className="inline-flex items-center px-4 py-2 bg-orange-500 text-white rounded-lg hover:bg-orange-600 no-underline font-medium">
  Descargar Colección Postman
</a>

La colección incluye:
- **Autenticación** - Flujo de login Auth0 preconfigurado
- **Track con GPS y Métricas** - Ejemplo básico de métricas OBD2
- **Track con Métricas en Batch** - Múltiples métricas con timestamps
- **Track con Telemetría Completa** - Ejemplo completo con todos los campos
- **Track con Códigos DTC** - Ejemplo de códigos de diagnóstico

---

## Ejemplos Completos

### Solicitud Mínima (Solo Campos Requeridos)

```bash
# Obtener token primero
TOKEN=$(curl -s --request POST \
  --url https://api.microboxlabs.com/api/v1/login \
  --header 'Content-Type: application/json' \
  --data '{
    "client_id": "TU_CLIENT_ID",
    "client_secret": "TU_CLIENT_SECRET",
    "audience": "https://iot.streamhub.cl/v1/asset/track",
    "grant_type": "client_credentials"
  }' | jq -r '.access_token')

# Enviar datos de seguimiento
curl --request POST \
  --url https://iot.streamhub.cl/v1/asset/track \
  --header "Authorization: Bearer $TOKEN" \
  --header 'Content-Type: application/json' \
  --header 'Accept: application/json' \
  --header 'X-Request-Id: req-001' \
  --header 'X-Request-Timestamp: 1705315800' \
  --data '{
    "asset_id": "A123456789",
    "timestamp": "2024-01-15T10:30:00Z"
  }'
```

### Con Ubicación GPS

```bash
curl --request POST \
  --url https://iot.streamhub.cl/v1/asset/track \
  --header "Authorization: Bearer $TOKEN" \
  --header 'Content-Type: application/json' \
  --header 'Accept: application/json' \
  --header 'X-Request-Id: req-002' \
  --header 'X-Request-Timestamp: 1705315800' \
  --data '{
    "asset_id": "A123456789",
    "timestamp": "2024-01-15T10:30:00Z",
    "type": "Truck",
    "gps": {
      "latitude": -33.4489,
      "longitude": -70.6693,
      "speed": 65.5,
      "heading": 180
    }
  }'
```

### Con Métricas Canónicas (OBD-II/J1939)

```bash
curl --request POST \
  --url https://iot.streamhub.cl/v1/asset/track \
  --header "Authorization: Bearer $TOKEN" \
  --header 'Content-Type: application/json' \
  --header 'Accept: application/json' \
  --header 'X-Request-Id: req-003' \
  --header 'X-Request-Timestamp: 1705315800' \
  --data '{
    "asset_id": "A123456789",
    "timestamp": "2024-01-15T10:30:00Z",
    "type": "Truck",
    "gps": {
      "latitude": -33.4489,
      "longitude": -70.6693,
      "speed": 65.5
    },
    "metrics": {
      "schema": "miot.metrics@1.0",
      "items": [
        { "k": "engine.rpm", "v": 2500, "u": "rpm", "src": "obd2", "q": "ok" },
        { "k": "vehicle.speed", "v": 72, "u": "km/h", "src": "obd2", "q": "ok" },
        { "k": "engine.coolant_temp", "v": 85, "u": "C", "src": "obd2", "q": "ok" },
        { "k": "fuel.level", "v": 65, "u": "%", "src": "obd2", "q": "ok" },
        { "k": "battery.voltage", "v": 14.2, "u": "V", "src": "obd2", "q": "ok" }
      ],
      "seq": 1001
    }
  }'
```

### Con Métricas en Batch (Múltiples Timestamps)

```bash
curl --request POST \
  --url https://iot.streamhub.cl/v1/asset/track \
  --header "Authorization: Bearer $TOKEN" \
  --header 'Content-Type: application/json' \
  --header 'Accept: application/json' \
  --header 'X-Request-Id: req-004' \
  --header 'X-Request-Timestamp: 1705315800' \
  --data '{
    "asset_id": "A123456789",
    "timestamp": "2024-01-15T10:30:30Z",
    "gps": {
      "latitude": -33.4489,
      "longitude": -70.6693,
      "speed": 58
    },
    "metrics": {
      "schema": "miot.metrics@1.0",
      "items": [
        { "k": "engine.rpm", "v": 1800, "u": "rpm", "ts": "2024-01-15T10:30:10Z", "src": "obd2" },
        { "k": "engine.rpm", "v": 1950, "u": "rpm", "ts": "2024-01-15T10:30:20Z", "src": "obd2" },
        { "k": "engine.rpm", "v": 2100, "u": "rpm", "ts": "2024-01-15T10:30:30Z", "src": "obd2" },
        { "k": "vehicle.speed", "v": 52, "u": "km/h", "ts": "2024-01-15T10:30:10Z", "src": "obd2" },
        { "k": "vehicle.speed", "v": 55, "u": "km/h", "ts": "2024-01-15T10:30:20Z", "src": "obd2" },
        { "k": "vehicle.speed", "v": 58, "u": "km/h", "ts": "2024-01-15T10:30:30Z", "src": "obd2" }
      ],
      "seq": 1002,
      "device_ts": "2024-01-15T10:30:30Z"
    }
  }'
```

### Con Códigos DTC (Códigos de Diagnóstico)

```bash
curl --request POST \
  --url https://iot.streamhub.cl/v1/asset/track \
  --header "Authorization: Bearer $TOKEN" \
  --header 'Content-Type: application/json' \
  --header 'Accept: application/json' \
  --header 'X-Request-Id: req-005' \
  --header 'X-Request-Timestamp: 1705315800' \
  --data '{
    "asset_id": "A123456789",
    "timestamp": "2024-01-15T10:30:00Z",
    "gps": {
      "latitude": -33.4489,
      "longitude": -70.6693,
      "speed": 45
    },
    "metrics": {
      "schema": "miot.metrics@1.0",
      "items": [
        { "k": "engine.rpm", "v": 2400, "u": "rpm", "src": "obd2", "q": "ok" },
        { "k": "engine.coolant_temp", "v": 105, "u": "C", "src": "obd2", "q": "ok" },
        { "k": "dtc.mil_on", "v": true, "src": "obd2", "q": "ok" },
        { "k": "dtc.count", "v": 2, "src": "obd2", "q": "ok" },
        {
          "k": "dtc.codes",
          "v": ["P0217", "P0128"],
          "src": "obd2",
          "q": "ok",
          "meta": {
            "descriptions": {
              "P0217": "Condición de sobretemperatura del refrigerante",
              "P0128": "Termostato por debajo de temperatura de regulación"
            }
          }
        }
      ],
      "seq": 1003
    }
  }'
```

### Con Sensores Conectados al GPS Directamente (Sin OBD-II/CAN BUS)

Para dispositivos GPS que proporcionan odómetro y estado del motor directamente:

```bash
curl --request POST \
  --url https://iot.streamhub.cl/v1/asset/track \
  --header "Authorization: Bearer $TOKEN" \
  --header 'Content-Type: application/json' \
  --header 'Accept: application/json' \
  --header 'X-Request-Id: req-004' \
  --header 'X-Request-Timestamp: 1705315800' \
  --data '{
    "asset_id": "A123456789",
    "timestamp": "2024-01-15T10:30:00Z",
    "gps": {
      "latitude": -33.4489,
      "longitude": -70.6693,
      "speed": 65.5,
      "heading": 180
    },
    "sensors": {
      "odometer": 125432.5,
      "engine_status": 1
    }
  }'
```

### Ejemplo en Python

```python
import requests
import time
from microboxlabs_auth_manager import AuthToken

# Inicializar autenticación
auth = AuthToken(
    client_id="TU_CLIENT_ID",
    client_secret="TU_CLIENT_SECRET",
    audience="https://iot.streamhub.cl/v1/asset/track",
    grant_type="client_credentials"
)

# Enviar datos de seguimiento con métricas
response = requests.post(
    "https://iot.streamhub.cl/v1/asset/track",
    headers={
        "Authorization": f"Bearer {auth.get_token()}",
        "Content-Type": "application/json",
        "Accept": "application/json",
        "X-Request-Id": f"req-{int(time.time())}",
        "X-Request-Timestamp": str(int(time.time()))
    },
    json={
        "asset_id": "A123456789",
        "timestamp": "2024-01-15T10:30:00Z",
        "gps": {
            "latitude": -33.4489,
            "longitude": -70.6693,
            "speed": 65.5
        },
        "metrics": {
            "schema": "miot.metrics@1.0",
            "items": [
                {"k": "engine.rpm", "v": 2500, "u": "rpm", "src": "obd2", "q": "ok"},
                {"k": "vehicle.speed", "v": 72, "u": "km/h", "src": "obd2", "q": "ok"},
                {"k": "engine.coolant_temp", "v": 85, "u": "C", "src": "obd2", "q": "ok"},
                {"k": "fuel.level", "v": 65, "u": "%", "src": "obd2", "q": "ok"}
            ],
            "seq": 1001
        }
    }
)

print(response.json())
```

---

## Métricas Comunes

Referencia rápida para métricas canónicas frecuentemente usadas:

| Clave | Descripción | Unidad | Fuente |
|-------|-------------|--------|--------|
| `engine.rpm` | Velocidad del motor | rpm | OBD PID 0C |
| `vehicle.speed` | Velocidad del vehículo | km/h | OBD PID 0D |
| `fuel.level` | Nivel del tanque de combustible | % | OBD PID 2F |
| `engine.coolant_temp` | Temperatura del refrigerante | C | OBD PID 05 |
| `engine.load` | Carga calculada del motor | % | OBD PID 04 |
| `battery.voltage` | Voltaje de batería/sistema | V | OBD PID 42 |
| `vehicle.odometer` | Distancia total | km | OBD PID 31 |
| `throttle.position` | Apertura del acelerador | % | OBD PID 11 |
| `dtc.mil_on` | Estado de luz check engine | bool | OBD PID 01 |
| `dtc.codes` | Códigos de falla activos | array | OBD Mode 03 |

→ Ver **Referencia > Registro de Métricas** para el catálogo completo de métricas canónicas.

---

## Manejo de Errores

### Lógica de Reintentos

Implementa backoff exponencial para errores transitorios:

```python
import time
import requests

def enviar_con_reintento(url, headers, data, max_reintentos=3):
    for intento in range(max_reintentos):
        try:
            response = requests.post(url, headers=headers, json=data)

            if response.status_code == 200:
                return response.json()
            elif response.status_code == 401:
                # Token expirado - renovar y reintentar
                headers["Authorization"] = f"Bearer {obtener_nuevo_token()}"
            elif response.status_code >= 500:
                # Error del servidor - reintentar con backoff
                time.sleep(2 ** intento)
            else:
                # Error del cliente - no reintentar
                return response.json()

        except requests.exceptions.RequestException:
            time.sleep(2 ** intento)

    raise Exception("Máximo de reintentos excedido")
```

### Errores de Validación

Cuando recibas un error de validación, revisa el objeto `errors` para problemas específicos de campos:

```json
{
  "status": "validation_error",
  "message": "Request validation failed",
  "errors": {
    "assetId": "Asset ID is required and cannot be blank"
  }
}
```

Corrige los campos indicados y reintenta.
