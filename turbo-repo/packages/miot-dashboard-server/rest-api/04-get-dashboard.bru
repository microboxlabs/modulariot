meta {
  name: Load a dashboard config
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/tenants/{{tenant}}/scopes/{{scopeId}}/dashboards/{{slug}}
  body: none
  auth: none
}

headers {
  x-dev-user: {{user}}
}

assert {
  res.status: eq 200
  res.body.data.version: eq 2
}
