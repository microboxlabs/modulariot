# Generated by packages/miot-dashboard-contract/scripts/generate-json-schema.ts,
# and compared byte for byte against a fresh generation by that package's
# tests. Prettier collapses its short arrays onto one line, which makes that
# comparison fail — and the comparison is what stops the committed artifact
# drifting from the zod schemas it is generated from, so the formatter gives
# way to it rather than the other way round.
#
# The repository's own `npm run format` only matches ts/tsx/md, so this covers
# someone running prettier over JSON by hand.
packages/miot-dashboard-contract/contract/dashboard-config.schema.json
