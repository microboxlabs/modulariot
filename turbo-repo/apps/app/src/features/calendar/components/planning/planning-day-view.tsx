// PlanningDayView now lives in @microboxlabs/miot-calendar-ui (rendered via the
// generic PlanningCalendar switch). This thin shim keeps the old import path
// resolving for any remaining importers.
export { PlanningDayView as default } from "@microboxlabs/miot-calendar-ui";
