# Local Workspace

This directory documents the runtime workspace layout. The default local
workspace is `.miot-workspace/`, which is gitignored.

```text
approvals/   approval requests and decisions
artifacts/   generated story, graph, and dashboard artifacts
evidence/    large evidence payloads kept outside model context
runs/        run records and event traces
stories/     Storytelling drafts
```

