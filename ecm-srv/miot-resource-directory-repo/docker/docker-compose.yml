version: '3.4'
services:
#  Optional
#  miot-resource-directory-repo-share:
#    image: ${docker.share.image}:${alfresco.share.version}
#    environment:
#      REPO_HOST: miot-resource-directory-repo-acs
#      REPO_PORT: 8080
#    ports:
#      - "${share.port}:8080"
  miot-resource-directory-repo-acs:
    image: alfresco-content-services-miot-resource-directory-repo:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../target
    environment:
      JAVA_TOOL_OPTIONS: "${keystore.settings}"
      JAVA_OPTS: "
        -Dsolr.secureComms=secret
        -Dsolr.sharedSecret=secret
        -Dmessaging.broker.url=\"failover:(nio://miot-resource-directory-repo-activemq:61616)?timeout=3000&jms.useCompression=true\"
        "
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - miot-resource-directory-repo-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - miot-resource-directory-repo-postgres
  miot-resource-directory-repo-postgres:
    image: postgres:16.5
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - miot-resource-directory-repo-db-volume:/var/lib/postgresql/data
  miot-resource-directory-repo-ass:
    image: alfresco/alfresco-search-services:2.0.15
    environment:
      SOLR_ALFRESCO_HOST: miot-resource-directory-repo-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: miot-resource-directory-repo-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
      ALFRESCO_SECURE_COMMS: "secret"
      JAVA_TOOL_OPTIONS: "-Dalfresco.secureComms.secret=secret"
    ports:
      - "8983:8983"
    volumes:
      - miot-resource-directory-repo-ass-volume:/opt/alfresco-search-services/contentstore
      - miot-resource-directory-repo-ass-volume:/opt/alfresco-search-services/data
  miot-resource-directory-repo-activemq:
      image: alfresco/alfresco-activemq:5.18-jre17-rockylinux8
      mem_limit: 1g
      ports:
        - 8161:8161 # Web Console
        - 5672:5672 # AMQP
        - 61616:61616 # OpenWire
        - 61613:61613 # STOMP

volumes:
  miot-resource-directory-repo-acs-volume:
    external: true
  miot-resource-directory-repo-db-volume:
    external: true
  miot-resource-directory-repo-ass-volume:
    external: true
